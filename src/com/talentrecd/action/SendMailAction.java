package com.talentrecd.action;

import com.talentrecd.model.Hr;

public class SendMailAction {
	/*public String AdminChangeRec() throws IOException{*/
		/*Hr hr=(Hr) session.getAttribute("hr");
		if(tbUsersinfo!=null)
		{
		checknumber=RanDomNumber.generateString();
		System.out.println(checknumber);
		email=iUsersInfoService.selectEmail(tbUsersinfo.getUsername());
		System.out.println(email);
		try {
			MailUtil.sendMail(email,"找回交易密码","您的验证码为:"+checknumber);
		} catch (MessagingException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
		return "JSON";
		}
		return "Direct";
	}*/
}
